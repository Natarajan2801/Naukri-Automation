name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 2 * * *' # Adjust as necessary

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Run tests
        run: echo 'hello'
